# Print-ready files

Generated by `scripts/print-export.py` from the orange-canonical
`design.es.svg` and `back.svg` of each design.

## Files

| File | Print size | Source |
|---|---|---|
| `altman-fin-del-mundo-front.svg` | 24 × 24 cm | `designs/altman-fin-del-mundo/design.es.svg` |
| `altman-fin-del-mundo-back.svg`  | 20 × 22 cm | `designs/altman-fin-del-mundo/back.svg` |
| `cais-extincion-front.svg`       | 24 × 24 cm | `designs/cais-extincion/design.es.svg` |
| `cais-extincion-back.svg`        | 20 × 22 cm | `designs/cais-extincion/back.svg` |
| `p-doom-evidencia-front.svg`     | 24 × 24 cm | `designs/p-doom-evidencia/design.es.svg` |
| `p-doom-evidencia-back.svg`      | 20 × 22 cm | `designs/p-doom-evidencia/back.svg` |
| `preguntame-front.svg`           | 24 × 24 cm | `designs/preguntame/design.es.svg` |
| `preguntame-back.svg`            | 20 × 22 cm | `designs/preguntame/back.svg` |

All eight files target the **orange-tee canonical** colours (white +
ink on orange). The white-tee and black-tee variants live next to
the originals in `designs/<slug>/` if needed later.

## Font handling

**Every `<text>` element has been replaced by `<path>` outlines** of
the Saira Condensed Bold glyphs from `brand/fonts/files/`. The print
files have **zero font dependency** — the printer does not need Saira
Condensed (or any other font) installed.

Italic style for the attribution lines in `cais-extincion-front.svg`
is synthesized with a `skewX(-10°)` transform on the run group (the
chapter only commits the regular weight to the repo).

## Sending to the printer

The SVGs are valid, standalone, and contain only paths + (in the
backs) embedded raster module rects for the QR. Any modern print
shop accepts SVG directly.

If your printer asks for **PDF** instead, convert with **macOS
Preview**:

1. Open the SVG in Safari or Chrome
2. File → Print (`⌘P`)
3. Bottom-left dropdown → **Save as PDF**
4. Save with the same name as `.pdf`

Or, from any vector tool (Affinity Designer, Sketch, online tools),
open the SVG and export → PDF.

## Regenerate

```sh
python3 scripts/print-export.py
```

Run after editing any `design.es.svg` or `back.svg`.
